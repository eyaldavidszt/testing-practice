import reverse from "./reverse";

test("something to return gnihtemos", () => {
  expect(reverse("something")).toBe("gnihtemos");
});

test("encyclopedia to return aidepolcycne", () => {
  expect(reverse("encyclopedia")).toBe("aidepolcycne");
});
